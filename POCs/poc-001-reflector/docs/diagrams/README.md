# Reflector diagrams

Archify interactive HTML (primary toolkit: `tools/diagrams/archify/`, local validate/deliver only — no Anthropic/OpenRouter).  
Source JSON lives in `_src/`. Story limited to `docs/context/gtm-before-after-brief.md` and the live deck.

**Product name:** Reflector (eng artifacts / HTML labels).  
**Deck:** `docs/slides/Reflector_Pitch_Deck.pptx` — slide 1 may still title “Reflected”; use Reflector in diagrams.

| File | What |
|------|------|
| `before-after.html` | Current vs Future state |
| `eight-beat-loop.html` | Eight-beat discovery loop |
| `_src/*.json` | Archify source specs |

## Look

- Pitch / hype / deck framing: green accent on cream  
- Product UI: Watermelon (not these diagram pages)

Large visual-check PNGs and the PPTX are intentionally omitted from git; prefer HTML + `_src` JSON + this README.
